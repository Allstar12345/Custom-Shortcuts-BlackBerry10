APP_NAME = HomescreenShortcuts
LIBS += -lbbcascadespickers
 LIBS += -lbbdevice
 LIBS += -lbbplatform
 LIBS += -lbbsystem
    LIBS += -lcamapi
    LIBS += -lbbpim
 

CONFIG += qt warn_on cascades10

include(config.pri)
